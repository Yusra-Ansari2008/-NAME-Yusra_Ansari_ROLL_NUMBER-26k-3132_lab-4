#include<stdio.h>
int main(){
	int number;
	int remainder;
	printf("Enter your number:");
	scanf("%d",&number);
	remainder=number%2;
	if(remainder==0)
	 {
	    printf("Your number is even");
	 }else{
		printf("Your number is odd");
	}
}

